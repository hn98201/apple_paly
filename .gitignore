# ────────────────────────────────────────────────
# ★보안: 소스코드(텔레그램 토큰 포함)·데이터·로그 전부 무시.
#   공개 GitHub Pages엔 생성된 HTML(docs/)만 올린다.
# ────────────────────────────────────────────────
/*
# 아래만 공유(화이트리스트) — docs(생성 HTML)만
!/docs/
!/.gitignore
